{
  "claude": { "cliVersion": "2.1.278", "capturedAt": "2026-09-21", "note": "one tool-using turn (Read); memory_paths.auto in the init line redacted from the user's home to /Users/example, and the scratch cwd to /tmp — fields konvoy never reads" },
  "claude-thinking": { "cliVersion": "2.1.278", "capturedAt": "2026-09-19", "note": "the original trivial turn, kept because it carries a thinking block the tool-using capture does not; same home-path redaction as claude" },
  "claude-error": { "cliVersion": "2.1.278", "capturedAt": "2026-09-21", "note": "resume of a nonexistent id: exit 1, is_error with no result text; the wording is on stderr" },
  "codex": { "cliVersion": "0.155.1", "capturedAt": "2026-09-21", "note": "one tool-using turn (command_execution); carries codex's skills-budget notice as an error item on a successful turn" },
  "codex-rate": { "cliVersion": "0.155.1", "capturedAt": "2026-09-23", "note": "a real exhausted-quota refusal, recorded during a smoke run and costing no quota: the skills-budget notice arrives as an error ITEM, then the failure arrives twice, as a top-level error line and inside turn.failed. Design §33 had this as uncapturable because a rate limit cannot be forced" },
  "kiro": { "cliVersion": "2.22.1", "capturedAt": "2026-09-21", "note": "one tool-using turn (fs_read), v2 engine; the scratch cwd redacted to /tmp" },
  "opencode": { "cliVersion": "2.0.11", "capturedAt": "2026-09-21", "note": "one tool-using turn (read); re-captured after the CLI moved from 2.0.10 during the audit" }
}
